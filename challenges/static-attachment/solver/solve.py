#!/usr/bin/env python3
"""Reference solver for the static-attachment challenge.

For a files-only challenge the "solver" documents how to derive the flag
from what's in ./dist — so a reviewer (and future-you) can confirm the
challenge still works after edits. Replace the body with the real steps.

    # e.g. for a crypto challenge:
    #   ct = open("../dist/cipher.bin", "rb").read()
    #   flag = decrypt(ct, key)
    #   assert flag.startswith("flag{")
    #   print(flag)
"""
print("describe how to recover the flag from ./dist here")
